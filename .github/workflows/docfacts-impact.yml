# Report-only: comments on PRs that touch code cited as evidence by documented
# claims. Never blocks a merge. Requires the DOCFACTS_READ_TOKEN secret.
name: docfacts-impact
on:
  pull_request:
    branches: [main]
permissions:
  pull-requests: write
  contents: read
jobs:
  impact:
    uses: primitivedotdev/docfacts/.github/workflows/impact.yml@0a45f0bbd17a93cc40a42159737cf11c4c2a84cf # pin: docfacts main; bump deliberately
    with:
      repo_key: sdks
    secrets:
      docfacts_read_token: ${{ secrets.DOCFACTS_READ_TOKEN }}
