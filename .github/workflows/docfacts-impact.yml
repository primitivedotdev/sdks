# Report-only: comments on PRs that touch code cited as evidence by documented
# claims. Never blocks a merge. Requires the DOCFACTS_READ_TOKEN secret.
name: docfacts-impact
on:
  pull_request:
    branches: [main]
permissions:
  pull-requests: write
  contents: read
jobs:
  impact:
    uses: primitivedotdev/docfacts/.github/workflows/impact.yml@61d6221efaca5eb8ac6383178385441114bf8948 # pin: docfacts main; bump deliberately
    with:
      repo_key: sdks
    secrets:
      docfacts_read_token: ${{ secrets.DOCFACTS_READ_TOKEN }}
