// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client/index.js';
import { client } from './client.gen.js';
import type { AddDomainData, AddDomainErrors, AddDomainResponses, CliLogoutData, CliLogoutErrors, CliLogoutResponses, CreateAgentAccountData, CreateAgentAccountErrors, CreateAgentAccountResponses, CreateAgentClaimLinkData, CreateAgentClaimLinkErrors, CreateAgentClaimLinkResponses, CreateChallengeData, CreateChallengeErrors, CreateChallengeResponses, CreateEmailChallengeData, CreateEmailChallengeErrors, CreateEmailChallengeResponses, CreateEndpointData, CreateEndpointErrors, CreateEndpointResponses, CreateFilterData, CreateFilterErrors, CreateFilterResponses, CreateFunctionData, CreateFunctionErrors, CreateFunctionResponses, CreateFunctionSecretData, CreateFunctionSecretErrors, CreateFunctionSecretResponses, CreateOrgSecretData, CreateOrgSecretErrors, CreateOrgSecretResponses, CreateRegistryData, CreateRegistryErrors, CreateRegistryResponses, CreateRouteData, CreateRouteErrors, CreateRouteResponses, CreateWakeAuthorizationData, CreateWakeAuthorizationErrors, CreateWakeAuthorizationResponses, CreateWakeScheduleData, CreateWakeScheduleErrors, CreateWakeScheduleResponses, DecideRegistryRequestData, DecideRegistryRequestErrors, DecideRegistryRequestResponses, DefineAgentData, DefineAgentErrors, DefineAgentResponses, DeleteDomainData, DeleteDomainErrors, DeleteDomainResponses, DeleteEmailData, DeleteEmailErrors, DeleteEmailResponses, DeleteEndpointData, DeleteEndpointErrors, DeleteEndpointResponses, DeleteFilterData, DeleteFilterErrors, DeleteFilterResponses, DeleteFunctionData, DeleteFunctionErrors, DeleteFunctionResponses, DeleteFunctionSecretData, DeleteFunctionSecretErrors, DeleteFunctionSecretResponses, DeleteMemoryData, DeleteMemoryErrors, DeleteMemoryResponses, DeleteOrgSecretData, DeleteOrgSecretErrors, DeleteOrgSecretResponses, DeleteRegistryData, DeleteRegistryErrors, DeleteRegistryResponses, DeleteRouteData, DeleteRouteErrors, DeleteRouteResponses, DeleteWakeAuthorizationData, DeleteWakeAuthorizationErrors, DeleteWakeAuthorizationResponses, DeleteWakeScheduleData, DeleteWakeScheduleErrors, DeleteWakeScheduleResponses, DiscardEmailContentData, DiscardEmailContentErrors, DiscardEmailContentResponses, DownloadAttachmentsData, DownloadAttachmentsErrors, DownloadAttachmentsResponses, DownloadDomainZoneFileData, DownloadDomainZoneFileErrors, DownloadDomainZoneFileResponses, DownloadRawEmailData, DownloadRawEmailErrors, DownloadRawEmailResponses, GetAccountData, GetAccountErrors, GetAccountResponses, GetAgentData, GetAgentErrors, GetAgentResponses, GetChallengeData, GetChallengeErrors, GetChallengeResponses, GetConversationData, GetConversationErrors, GetConversationResponses, GetEmailData, GetEmailErrors, GetEmailResponses, GetFunctionData, GetFunctionErrors, GetFunctionResponses, GetFunctionRoutingData, GetFunctionRoutingErrors, GetFunctionRoutingResponses, GetFunctionTestRunTraceData, GetFunctionTestRunTraceErrors, GetFunctionTestRunTraceResponses, GetInboxStatusData, GetInboxStatusErrors, GetInboxStatusResponses, GetMemoryData, GetMemoryErrors, GetMemoryResponses, GetOrgRoutingTopologyData, GetOrgRoutingTopologyErrors, GetOrgRoutingTopologyResponses, GetRegistryData, GetRegistryErrors, GetRegistryResponses, GetSendPermissionsData, GetSendPermissionsErrors, GetSendPermissionsResponses, GetSentEmailData, GetSentEmailErrors, GetSentEmailResponses, GetSpendPolicyData, GetSpendPolicyErrors, GetSpendPolicyResponses, GetStorageStatsData, GetStorageStatsErrors, GetStorageStatsResponses, GetThreadData, GetThreadErrors, GetThreadResponses, GetWakeScheduleData, GetWakeScheduleErrors, GetWakeScheduleResponses, GetWebhookSecretData, GetWebhookSecretErrors, GetWebhookSecretResponses, ListDeclinedPaymentsData, ListDeclinedPaymentsErrors, ListDeclinedPaymentsResponses, ListDeliveriesData, ListDeliveriesErrors, ListDeliveriesResponses, ListDomainsData, ListDomainsErrors, ListDomainsResponses, ListEmailsData, ListEmailsErrors, ListEmailsResponses, ListEndpointsData, ListEndpointsErrors, ListEndpointsResponses, ListFiltersData, ListFiltersErrors, ListFiltersResponses, ListFunctionLogsData, ListFunctionLogsErrors, ListFunctionLogsResponses, ListFunctionsData, ListFunctionSecretsData, ListFunctionSecretsErrors, ListFunctionSecretsResponses, ListFunctionsErrors, ListFunctionsResponses, ListOrgSecretsData, ListOrgSecretsErrors, ListOrgSecretsResponses, ListPayoutAddressesData, ListPayoutAddressesErrors, ListPayoutAddressesResponses, ListRegistriesData, ListRegistriesErrors, ListRegistriesResponses, ListRegistryAgentsData, ListRegistryAgentsResponses, ListRegistryRequestsData, ListRegistryRequestsErrors, ListRegistryRequestsResponses, ListRoutesData, ListRoutesErrors, ListRoutesResponses, ListSentEmailsData, ListSentEmailsErrors, ListSentEmailsResponses, ListWakeAuthorizationsData, ListWakeAuthorizationsErrors, ListWakeAuthorizationsResponses, ListWakeDispatchesData, ListWakeDispatchesErrors, ListWakeDispatchesResponses, ListWakeSchedulesData, ListWakeSchedulesErrors, ListWakeSchedulesResponses, PayChallengeData, PayChallengeErrors, PayChallengeResponses, PollCliLoginData, PollCliLoginErrors, PollCliLoginResponses, PublishAgentData, PublishAgentErrors, PublishAgentResponses, RegisterPayoutAddressData, RegisterPayoutAddressErrors, RegisterPayoutAddressResponses, ReorderRoutesData, ReorderRoutesErrors, ReorderRoutesResponses, ReplayDeliveryData, ReplayDeliveryErrors, ReplayDeliveryResponses, ReplayEmailWebhooksData, ReplayEmailWebhooksErrors, ReplayEmailWebhooksResponses, ReplyToEmailData, ReplyToEmailErrors, ReplyToEmailResponses, ResendAgentSignupVerificationData, ResendAgentSignupVerificationErrors, ResendAgentSignupVerificationResponses, ResendCliSignupVerificationData, ResendCliSignupVerificationErrors, ResendCliSignupVerificationResponses, ResolveRegistryHandleData, ResolveRegistryHandleErrors, ResolveRegistryHandleResponses, RotateWebhookSecretData, RotateWebhookSecretErrors, RotateWebhookSecretResponses, RunWakeScheduleData, RunWakeScheduleErrors, RunWakeScheduleResponses, SearchEmailsData, SearchEmailsErrors, SearchEmailsResponses, SearchMemoriesData, SearchMemoriesErrors, SearchMemoriesResponses, SemanticSearchData, SemanticSearchErrors, SemanticSearchResponses, SendEmailData, SendEmailErrors, SendEmailResponses, SetFunctionRouteData, SetFunctionRouteErrors, SetFunctionRouteResponses, SetFunctionSecretData, SetFunctionSecretErrors, SetFunctionSecretResponses, SetMemoryData, SetMemoryErrors, SetMemoryResponses, SetOrgSecretData, SetOrgSecretErrors, SetOrgSecretResponses, SimulateRouteData, SimulateRouteErrors, SimulateRouteResponses, StartAgentClaimData, StartAgentClaimErrors, StartAgentClaimResponses, StartAgentSignupData, StartAgentSignupErrors, StartAgentSignupResponses, StartCliLoginData, StartCliLoginErrors, StartCliLoginResponses, StartCliSignupData, StartCliSignupErrors, StartCliSignupResponses, TestEndpointData, TestEndpointErrors, TestEndpointResponses, TestFunctionData, TestFunctionErrors, TestFunctionResponses, UnpublishAgentData, UnpublishAgentErrors, UnpublishAgentResponses, UnsetFunctionRouteData, UnsetFunctionRouteErrors, UnsetFunctionRouteResponses, UpdateAccountData, UpdateAccountErrors, UpdateAccountResponses, UpdateDomainData, UpdateDomainErrors, UpdateDomainResponses, UpdateEndpointData, UpdateEndpointErrors, UpdateEndpointResponses, UpdateFilterData, UpdateFilterErrors, UpdateFilterResponses, UpdateFunctionData, UpdateFunctionErrors, UpdateFunctionResponses, UpdateRegistryData, UpdateRegistryErrors, UpdateRegistryResponses, UpdateRouteData, UpdateRouteErrors, UpdateRouteResponses, UpdateSpendPolicyData, UpdateSpendPolicyErrors, UpdateSpendPolicyResponses, UpdateWakeAuthorizationData, UpdateWakeAuthorizationErrors, UpdateWakeAuthorizationResponses, UpdateWakeScheduleData, UpdateWakeScheduleErrors, UpdateWakeScheduleResponses, VerifyAgentClaimData, VerifyAgentClaimErrors, VerifyAgentClaimResponses, VerifyAgentSignupData, VerifyAgentSignupErrors, VerifyAgentSignupResponses, VerifyCliSignupData, VerifyCliSignupErrors, VerifyCliSignupResponses, VerifyDomainData, VerifyDomainErrors, VerifyDomainResponses } from './types.gen.js';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Start CLI browser login
 *
 * Starts a browser-assisted CLI login session. The response includes a
 * device code for polling and a user code that the user approves in the
 * browser. This endpoint does not require an API key.
 *
 */
export const startCliLogin = <ThrowOnError extends boolean = false>(options?: Options<StartCliLoginData, ThrowOnError>) => (options?.client ?? client).post<StartCliLoginResponses, StartCliLoginErrors, ThrowOnError>({
    url: '/cli/login/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Poll CLI browser login
 *
 * Polls a CLI login session until the browser approval either succeeds,
 * is denied, expires, or is polled too quickly. The OAuth token set is
 * created only after approval and is returned exactly once.
 *
 */
export const pollCliLogin = <ThrowOnError extends boolean = false>(options: Options<PollCliLoginData, ThrowOnError>) => (options.client ?? client).post<PollCliLoginResponses, PollCliLoginErrors, ThrowOnError>({
    url: '/cli/login/poll',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Start CLI account signup
 *
 * Starts a terminal-native CLI signup. `signup_code` is optional;
 * omit it to sign up without one. The API creates a pending signup
 * session, sends an email verification code, and returns an opaque
 * signup token used by the resend and verify steps. This endpoint
 * does not require an API key.
 *
 */
export const startCliSignup = <ThrowOnError extends boolean = false>(options: Options<StartCliSignupData, ThrowOnError>) => (options.client ?? client).post<StartCliSignupResponses, StartCliSignupErrors, ThrowOnError>({
    url: '/cli/signup/start',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Resend CLI signup verification code
 *
 * Sends a new email verification code for a pending CLI signup session.
 * This endpoint does not require an API key.
 *
 */
export const resendCliSignupVerification = <ThrowOnError extends boolean = false>(options: Options<ResendCliSignupVerificationData, ThrowOnError>) => (options.client ?? client).post<ResendCliSignupVerificationResponses, ResendCliSignupVerificationErrors, ThrowOnError>({
    url: '/cli/signup/resend',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Verify CLI signup and create OAuth session
 *
 * Verifies the email code for a CLI signup session and creates the
 * account. When the session was started with a `signup_code`, the
 * reserved code is redeemed; sessions started without a code skip
 * the redemption step. Either way an org-scoped OAuth CLI session
 * is created and the token set is returned exactly once. This
 * endpoint does not require an API key.
 *
 */
export const verifyCliSignup = <ThrowOnError extends boolean = false>(options: Options<VerifyCliSignupData, ThrowOnError>) => (options.client ?? client).post<VerifyCliSignupResponses, VerifyCliSignupErrors, ThrowOnError>({
    url: '/cli/signup/verify',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Start agent account signup
 *
 * Starts an agent-native signup session. `signup_code` is optional;
 * omit it to sign up without one. The API creates a pending signup
 * session, sends an email verification code, and returns an opaque
 * signup token used by the resend and verify steps. This endpoint
 * does not require an API key.
 *
 */
export const startAgentSignup = <ThrowOnError extends boolean = false>(options: Options<StartAgentSignupData, ThrowOnError>) => (options.client ?? client).post<StartAgentSignupResponses, StartAgentSignupErrors, ThrowOnError>({
    url: '/agent/signup/start',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Resend agent signup verification code
 *
 * Sends a new email verification code for a pending agent signup session.
 * This endpoint does not require an API key.
 *
 */
export const resendAgentSignupVerification = <ThrowOnError extends boolean = false>(options: Options<ResendAgentSignupVerificationData, ThrowOnError>) => (options.client ?? client).post<ResendAgentSignupVerificationResponses, ResendAgentSignupVerificationErrors, ThrowOnError>({
    url: '/agent/signup/resend',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Verify agent signup and create OAuth tokens
 *
 * Verifies the email code for an agent signup session and creates
 * the account when needed. When the session was started with a
 * `signup_code`, the reserved code is redeemed; sessions started
 * without a code skip the redemption step. An org-scoped OAuth
 * session for CLI authentication is minted and the raw tokens are
 * returned exactly once. For existing users, the optional `org_id`
 * selects which accessible workspace should receive the new
 * session (no signup-code redemption is performed for existing
 * users regardless of how the session was started).
 *
 */
export const verifyAgentSignup = <ThrowOnError extends boolean = false>(options: Options<VerifyAgentSignupData, ThrowOnError>) => (options.client ?? client).post<VerifyAgentSignupResponses, VerifyAgentSignupErrors, ThrowOnError>({
    url: '/agent/signup/verify',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Create an emailless agent account
 *
 * Creates an emailless agent account without authentication and returns a
 * one-time API key (prefixed `prim_`) plus a provisioned managed inbox.
 * The account is on the `agent` plan: reply-only (it can send only to
 * addresses that have already sent it authenticated mail) with tight send
 * limits. Use the returned `api_key` as a Bearer token on later calls. The
 * account can be upgraded to a full developer account by confirming an
 * email through the claim flow. This endpoint does not require an API key.
 *
 */
export const createAgentAccount = <ThrowOnError extends boolean = false>(options: Options<CreateAgentAccountData, ThrowOnError>) => (options.client ?? client).post<CreateAgentAccountResponses, CreateAgentAccountErrors, ThrowOnError>({
    url: '/agent/accounts',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Start an agent account email claim
 *
 * Begins upgrading an emailless `agent` account into a full `developer`
 * account by confirming an email address. Authenticated by the agent's own
 * API key (the org is taken from the credential). Sends a verification
 * code to the supplied email and returns the claim session id plus resend
 * timing. Submit the code to `/agent/claim/verify` to complete the
 * upgrade. Confirming an email that already belongs to a Primitive account
 * is rejected.
 *
 */
export const startAgentClaim = <ThrowOnError extends boolean = false>(options: Options<StartAgentClaimData, ThrowOnError>) => (options.client ?? client).post<StartAgentClaimResponses, StartAgentClaimErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/agent/claim/start',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Verify an agent account email claim
 *
 * Confirms the verification code emailed by `/agent/claim/start` and
 * upgrades the account to the `developer` plan. The org id, API key, and
 * managed inbox all carry over; the send cap lifts. Authenticated by the
 * agent's own API key.
 *
 */
export const verifyAgentClaim = <ThrowOnError extends boolean = false>(options: Options<VerifyAgentClaimData, ThrowOnError>) => (options.client ?? client).post<VerifyAgentClaimResponses, VerifyAgentClaimErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/agent/claim/verify',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Create a browser claim link
 *
 * Mints an opaque, single-use link an agent can hand to a human to
 * complete the email-confirmation upgrade in a browser. Authenticated by
 * the agent's own API key. `claim_url` is null when the API host cannot
 * resolve a web origin to build the link.
 *
 */
export const createAgentClaimLink = <ThrowOnError extends boolean = false>(options?: Options<CreateAgentClaimLinkData, ThrowOnError>) => (options?.client ?? client).post<CreateAgentClaimLinkResponses, CreateAgentClaimLinkErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/agent/claim/link',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Revoke the current CLI OAuth session
 *
 * Revokes the OAuth grant used to authenticate the request. API-key
 * authenticated legacy logout requests succeed without deleting server API
 * keys so old local CLI state can be cleared safely.
 *
 */
export const cliLogout = <ThrowOnError extends boolean = false>(options?: Options<CliLogoutData, ThrowOnError>) => (options?.client ?? client).post<CliLogoutResponses, CliLogoutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cli/logout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Get account info
 */
export const getAccount = <ThrowOnError extends boolean = false>(options?: Options<GetAccountData, ThrowOnError>) => (options?.client ?? client).get<GetAccountResponses, GetAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/account',
    ...options
});

/**
 * Update account settings
 */
export const updateAccount = <ThrowOnError extends boolean = false>(options: Options<UpdateAccountData, ThrowOnError>) => (options.client ?? client).patch<UpdateAccountResponses, UpdateAccountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/account',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Get storage usage
 */
export const getStorageStats = <ThrowOnError extends boolean = false>(options?: Options<GetStorageStatsData, ThrowOnError>) => (options?.client ?? client).get<GetStorageStatsResponses, GetStorageStatsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/account/storage',
    ...options
});

/**
 * Get webhook signing secret
 *
 * Returns the webhook signing secret for your account. If no
 * secret exists yet, one is generated automatically on first
 * access.
 *
 * Signing is account-scoped, not per-endpoint. Every webhook
 * delivery from any of your registered endpoints is signed
 * with this single secret. Rotate via
 * `POST /account/webhook-secret/rotate`.
 *
 * **Secret format**: the returned string looks base64-shaped
 * (e.g. `XNHBBW8VqoBjRfNs1tkZj11jTk...`) but is NOT base64.
 * Use it AS-IS as a UTF-8 string when computing HMAC over a
 * delivery body. Base64-decoding before HMAC will silently
 * produce mismatched signatures.
 *
 * See the API-level "Webhook signing" section for the full
 * wire format (header name, signed string shape, hash algo,
 * tolerance) including a language-agnostic verification
 * recipe.
 *
 */
export const getWebhookSecret = <ThrowOnError extends boolean = false>(options?: Options<GetWebhookSecretData, ThrowOnError>) => (options?.client ?? client).get<GetWebhookSecretResponses, GetWebhookSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/account/webhook-secret',
    ...options
});

/**
 * Rotate webhook signing secret
 *
 * Generates a new webhook signing secret, replacing the current one.
 * Rate limited to once per 60 minutes.
 *
 */
export const rotateWebhookSecret = <ThrowOnError extends boolean = false>(options?: Options<RotateWebhookSecretData, ThrowOnError>) => (options?.client ?? client).post<RotateWebhookSecretResponses, RotateWebhookSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/account/webhook-secret/rotate',
    ...options
});

/**
 * List all domains
 *
 * Returns all verified and unverified domains for your organization,
 * sorted by creation date (newest first). Each domain includes a
 * `verified` boolean to distinguish between the two states.
 *
 */
export const listDomains = <ThrowOnError extends boolean = false>(options?: Options<ListDomainsData, ThrowOnError>) => (options?.client ?? client).get<ListDomainsResponses, ListDomainsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/domains',
    ...options
});

/**
 * Claim a new domain
 *
 * Creates an unverified domain claim and returns the exact
 * DNS records to publish in `dns_records`. Publish those
 * records before calling the verify endpoint. To give users
 * an importable DNS file, call `downloadDomainZoneFile` or run
 * `primitive domains zone-file --id <domain-id>`.
 *
 */
export const addDomain = <ThrowOnError extends boolean = false>(options: Options<AddDomainData, ThrowOnError>) => (options.client ?? client).post<AddDomainResponses, AddDomainErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/domains',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a domain
 *
 * Deletes a verified or unverified domain claim.
 */
export const deleteDomain = <ThrowOnError extends boolean = false>(options: Options<DeleteDomainData, ThrowOnError>) => (options.client ?? client).delete<DeleteDomainResponses, DeleteDomainErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/domains/{id}',
    ...options
});

/**
 * Update domain settings
 *
 * Update a verified domain's settings. Only verified domains can be
 * updated. Per-domain spam thresholds require a Pro plan.
 *
 */
export const updateDomain = <ThrowOnError extends boolean = false>(options: Options<UpdateDomainData, ThrowOnError>) => (options.client ?? client).patch<UpdateDomainResponses, UpdateDomainErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/domains/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Verify domain ownership
 *
 * Checks DNS records required for inbound routing, ownership,
 * and outbound authentication: MX, ownership TXT, SPF, DKIM,
 * DMARC, and TLS-RPT.
 * On success, the domain is promoted from unverified to verified.
 * On failure, returns which checks passed and which failed,
 * plus the exact DNS records still expected. To give users
 * an importable DNS file for missing records, call
 * `downloadDomainZoneFile` or run
 * `primitive domains zone-file --id <domain-id>`.
 *
 */
export const verifyDomain = <ThrowOnError extends boolean = false>(options: Options<VerifyDomainData, ThrowOnError>) => (options.client ?? client).post<VerifyDomainResponses, VerifyDomainErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/domains/{id}/verify',
    ...options
});

/**
 * Download domain DNS zone file
 *
 * Downloads a BIND-format DNS zone file containing the DNS records
 * required for a domain claim. Agents should offer this after
 * `addDomain` when users want to import DNS records instead of
 * copying each record manually.
 *
 */
export const downloadDomainZoneFile = <ThrowOnError extends boolean = false>(options: Options<DownloadDomainZoneFileData, ThrowOnError>) => (options.client ?? client).get<DownloadDomainZoneFileResponses, DownloadDomainZoneFileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/domains/{id}/zone-file',
    ...options
});

/**
 * Get inbound inbox readiness
 *
 * Returns one consolidated view of inbound domain readiness,
 * webhook/function processing routes, deployed Functions, and
 * recent inbound email activity.
 *
 * Agents should call this before guiding a user through inbound
 * setup. It answers the practical questions "can I receive mail",
 * "will anything process that mail", and "what should I do next"
 * without forcing clients to stitch together domains, endpoints,
 * functions, and emails manually.
 *
 */
export const getInboxStatus = <ThrowOnError extends boolean = false>(options?: Options<GetInboxStatusData, ThrowOnError>) => (options?.client ?? client).get<GetInboxStatusResponses, GetInboxStatusErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/inbox/status',
    ...options
});

/**
 * List inbound emails
 *
 * Returns a paginated list of INBOUND emails received at your
 * verified domains. Outbound messages sent via /send-mail are
 * not included; this endpoint is the inbox view, not a
 * unified send/receive history.
 *
 * Supports filtering by domain, status, date range, and
 * free-text search across subject, sender, and recipient
 * fields.
 *
 * For a compact text-table summary of the most recent N
 * inbounds (no filters, no cursor pagination), the CLI ships
 * `primitive emails:latest` as a one-line-per-email shortcut.
 * It's TTY-aware so id columns are full UUIDs when piped, and
 * a `--json` flag returns the same envelope this endpoint
 * does. Use whichever fits the call site.
 *
 */
export const listEmails = <ThrowOnError extends boolean = false>(options?: Options<ListEmailsData, ThrowOnError>) => (options?.client ?? client).get<ListEmailsResponses, ListEmailsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails',
    ...options
});

/**
 * Search inbound emails
 *
 * Searches inbound emails with structured filters and optional
 * full-text matching across parsed email fields. This endpoint is
 * optimized for filtered inbox views and CLI polling workflows:
 * callers that only need new accepted mail can pass
 * `sort=received_at_asc`, `snippet=false`, `include_facets=false`,
 * and a `date_from` timestamp.
 *
 * `q`, `subject`, and `body` use the same English full-text index
 * as the web inbox search. Structured filters such as `from`, `to`,
 * `domain_id`, status, attachment presence, and spam score bounds
 * are combined with the text query.
 *
 */
export const searchEmails = <ThrowOnError extends boolean = false>(options?: Options<SearchEmailsData, ThrowOnError>) => (options?.client ?? client).get<SearchEmailsResponses, SearchEmailsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/search',
    ...options
});

/**
 * Delete an email
 */
export const deleteEmail = <ThrowOnError extends boolean = false>(options: Options<DeleteEmailData, ThrowOnError>) => (options.client ?? client).delete<DeleteEmailResponses, DeleteEmailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/{id}',
    ...options
});

/**
 * Get inbound email by id
 *
 * Returns the full record for an inbound email received at one
 * of your verified domains, including the parsed text and HTML
 * bodies, threading metadata, SMTP envelope detail, webhook
 * delivery state, and a `replies` array for any outbound sends
 * recorded as replies to this inbound.
 *
 * For listing inbound emails (with cursor pagination, status
 * and date filters, and free-text search), use
 * `/emails`. Outbound (sent) email records are NOT returned
 * here; use `/sent-emails/{id}` for those.
 *
 * The response carries four sender-shaped fields whose
 * meanings overlap. `from_email` is the canonical "who sent
 * this" field for most use cases (parsed bare address from
 * the `From:` header, with a `sender` fallback). `from_header`
 * is the raw header including any display name. `sender` and
 * `smtp_mail_from` both carry the SMTP envelope MAIL FROM
 * (return-path) and are equal by construction; `sender` is
 * the older field name retained for compatibility. See
 * `primitive describe emails:get-email | jq '.responseSchema.properties'`
 * for per-field detail.
 *
 */
export const getEmail = <ThrowOnError extends boolean = false>(options: Options<GetEmailData, ThrowOnError>) => (options.client ?? client).get<GetEmailResponses, GetEmailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/{id}',
    ...options
});

/**
 * Download raw email
 *
 * Downloads the raw RFC 822 email file (.eml). Authenticates via
 * a signed download token (provided in webhook payloads) or a
 * valid session.
 *
 */
export const downloadRawEmail = <ThrowOnError extends boolean = false>(options: Options<DownloadRawEmailData, ThrowOnError>) => (options.client ?? client).get<DownloadRawEmailResponses, DownloadRawEmailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, {
            in: 'query',
            name: 'token',
            type: 'apiKey'
        }],
    url: '/emails/{id}/raw',
    ...options
});

/**
 * Download email attachments
 *
 * Downloads all attachments as a gzip-compressed tar archive.
 * Authenticates via a signed download token (provided in webhook
 * payloads) or a valid session.
 *
 */
export const downloadAttachments = <ThrowOnError extends boolean = false>(options: Options<DownloadAttachmentsData, ThrowOnError>) => (options.client ?? client).get<DownloadAttachmentsResponses, DownloadAttachmentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }, {
            in: 'query',
            name: 'token',
            type: 'apiKey'
        }],
    url: '/emails/{id}/attachments.tar.gz',
    ...options
});

/**
 * Reply to an inbound email
 *
 * Sends an outbound reply to the inbound email identified by `id`.
 * Threading headers (`In-Reply-To`, `References`), recipient
 * derivation (Reply-To, then From, then bare sender), and the
 * `Re:` subject prefix are all derived server-side from the
 * stored inbound row. The request body carries only the message
 * body, optional From override, optional attachments, and optional
 * `wait` flag; passing any header or recipient override is
 * rejected by the schema (`additionalProperties: false`).
 *
 * Forwards through the same gates as `/send-mail`: the response
 * status, error envelope, and `idempotent_replay` flag mirror
 * the send-mail contract verbatim.
 *
 */
export const replyToEmail = <ThrowOnError extends boolean = false>(options: Options<ReplyToEmailData, ThrowOnError>) => (options.client ?? client).post<ReplyToEmailResponses, ReplyToEmailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/{id}/reply',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Replay email webhooks
 *
 * Re-delivers the webhook payload for this email to all active
 * endpoints matching the email's domain. Rate limited per-email
 * (short cooldown between successive replays of the same email)
 * and per-org (burst + sustained windows), sharing an org-wide
 * budget with delivery replays.
 *
 */
export const replayEmailWebhooks = <ThrowOnError extends boolean = false>(options: Options<ReplayEmailWebhooksData, ThrowOnError>) => (options.client ?? client).post<ReplayEmailWebhooksResponses, ReplayEmailWebhooksErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/{id}/replay',
    ...options
});

/**
 * Discard email content
 *
 * Permanently deletes the email's raw bytes, parsed body (text + HTML),
 * and attachments while preserving metadata (sender, recipient,
 * subject, timestamps, hashes, attachment manifest) for audit logs.
 * Idempotent: a second call returns success with
 * `already_discarded: true` and does no work.
 *
 * **Gated** on the customer's discard-content opt-in (managed in the
 * dashboard at Settings > Webhooks). When the toggle is off, this
 * endpoint returns `403` with code `discard_not_enabled` and a
 * message pointing the human at the dashboard. There is intentionally
 * no API to flip this toggle. Opting in to a destructive,
 * non-reversible operation must be a deliberate human click in the
 * UI.
 *
 */
export const discardEmailContent = <ThrowOnError extends boolean = false>(options: Options<DiscardEmailContentData, ThrowOnError>) => (options.client ?? client).post<DiscardEmailContentResponses, DiscardEmailContentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/{id}/discard-content',
    ...options
});

/**
 * Get the conversation an email belongs to
 *
 * Returns the full conversation the given inbound email belongs
 * to, as ordered, ready-to-prompt turns WITH bodies. It resolves
 * the thread from the email and returns every message oldest-first,
 * so an agent that received an email can pass `messages` straight
 * to a chat model in one call instead of walking `/threads/{id}`
 * plus `/emails/{id}` and `/sent-emails/{id}` per message.
 *
 * Each message carries a `direction` (`inbound` | `outbound`) and a
 * derived `role`: `inbound` -> `user`, `outbound` -> `assistant`
 * (your own prior replies). The role mapping assumes the caller
 * owns the outbound side, which is the agent-reply case this exists
 * for. If the email has no thread yet (a brand-new message), the
 * conversation is just that one message as a single user turn.
 *
 * The message list is capped; check `truncated` to detect when
 * older messages were omitted. Consecutive same-role turns are not
 * merged here; that normalization is model-specific and left to the
 * caller.
 *
 */
export const getConversation = <ThrowOnError extends boolean = false>(options: Options<GetConversationData, ThrowOnError>) => (options.client ?? client).get<GetConversationResponses, GetConversationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/emails/{id}/conversation',
    ...options
});

/**
 * List webhook endpoints
 *
 * Returns all active (non-deleted) webhook endpoints.
 */
export const listEndpoints = <ThrowOnError extends boolean = false>(options?: Options<ListEndpointsData, ThrowOnError>) => (options?.client ?? client).get<ListEndpointsResponses, ListEndpointsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/endpoints',
    ...options
});

/**
 * Create a webhook endpoint
 *
 * Creates a new webhook endpoint. If a deactivated endpoint
 * with the same URL and domain exists, it is reactivated
 * instead. Subject to plan limits on the number of active
 * endpoints.
 *
 * **Signing is account-scoped, not per-endpoint.** This call
 * does not return any signing material; every endpoint on the
 * account uses the same webhook secret, fetched via
 * `GET /account/webhook-secret`. See the API-level "Webhook
 * signing" section for the full wire format (header name,
 * signed string, hash algo, secret format, tolerance) and a
 * language-agnostic verification recipe.
 *
 * After creating the endpoint, fire a test delivery against
 * it via `POST /endpoints/{id}/test` to confirm your verifier
 * accepts the signature.
 *
 */
export const createEndpoint = <ThrowOnError extends boolean = false>(options: Options<CreateEndpointData, ThrowOnError>) => (options.client ?? client).post<CreateEndpointResponses, CreateEndpointErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/endpoints',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a webhook endpoint
 *
 * Soft-deletes a webhook endpoint. The endpoint will no longer
 * receive webhook deliveries.
 *
 */
export const deleteEndpoint = <ThrowOnError extends boolean = false>(options: Options<DeleteEndpointData, ThrowOnError>) => (options.client ?? client).delete<DeleteEndpointResponses, DeleteEndpointErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/endpoints/{id}',
    ...options
});

/**
 * Update a webhook endpoint
 *
 * Updates an active webhook endpoint. If the URL is changed, the old
 * endpoint is deactivated and a new one is created (or an existing
 * deactivated endpoint with the new URL is reactivated).
 *
 */
export const updateEndpoint = <ThrowOnError extends boolean = false>(options: Options<UpdateEndpointData, ThrowOnError>) => (options.client ?? client).patch<UpdateEndpointResponses, UpdateEndpointErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/endpoints/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Send a test webhook
 *
 * Sends a sample `email.received` event to the endpoint. The request
 * includes SSRF protection (private IP rejection and DNS pinning).
 * Rate limited to 4 per minute and 30 per hour (non-exempt).
 * Successful deliveries and verified-domain endpoints are exempt
 * from the rate limit.
 *
 */
export const testEndpoint = <ThrowOnError extends boolean = false>(options: Options<TestEndpointData, ThrowOnError>) => (options.client ?? client).post<TestEndpointResponses, TestEndpointErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/endpoints/{id}/test',
    ...options
});

/**
 * List filter rules
 *
 * Returns all whitelist and blocklist filter rules.
 */
export const listFilters = <ThrowOnError extends boolean = false>(options?: Options<ListFiltersData, ThrowOnError>) => (options?.client ?? client).get<ListFiltersResponses, ListFiltersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/filters',
    ...options
});

/**
 * Create a filter rule
 *
 * Creates a new whitelist or blocklist filter. Per-domain filters
 * require a Pro plan. Patterns are stored as lowercase.
 *
 */
export const createFilter = <ThrowOnError extends boolean = false>(options: Options<CreateFilterData, ThrowOnError>) => (options.client ?? client).post<CreateFilterResponses, CreateFilterErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/filters',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a filter rule
 */
export const deleteFilter = <ThrowOnError extends boolean = false>(options: Options<DeleteFilterData, ThrowOnError>) => (options.client ?? client).delete<DeleteFilterResponses, DeleteFilterErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/filters/{id}',
    ...options
});

/**
 * Update a filter rule
 *
 * Toggle a filter's enabled state.
 */
export const updateFilter = <ThrowOnError extends boolean = false>(options: Options<UpdateFilterData, ThrowOnError>) => (options.client ?? client).patch<UpdateFilterResponses, UpdateFilterErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/filters/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List wake schedules
 *
 * Returns the org's wake.dispatch schedules.
 */
export const listWakeSchedules = <ThrowOnError extends boolean = false>(options?: Options<ListWakeSchedulesData, ThrowOnError>) => (options?.client ?? client).get<ListWakeSchedulesResponses, ListWakeSchedulesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/schedules',
    ...options
});

/**
 * Create a wake schedule
 *
 * Create a cron schedule that sends a wake.dispatch command to one of your
 * own function addresses. `from` and `to` must differ (no self-dispatch);
 * the cron expression and IANA timezone are validated and the first fire
 * time is computed without firing immediately.
 *
 */
export const createWakeSchedule = <ThrowOnError extends boolean = false>(options: Options<CreateWakeScheduleData, ThrowOnError>) => (options.client ?? client).post<CreateWakeScheduleResponses, CreateWakeScheduleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/schedules',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a wake schedule
 */
export const deleteWakeSchedule = <ThrowOnError extends boolean = false>(options: Options<DeleteWakeScheduleData, ThrowOnError>) => (options.client ?? client).delete<DeleteWakeScheduleResponses, DeleteWakeScheduleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/schedules/{id}',
    ...options
});

/**
 * Get a wake schedule
 */
export const getWakeSchedule = <ThrowOnError extends boolean = false>(options: Options<GetWakeScheduleData, ThrowOnError>) => (options.client ?? client).get<GetWakeScheduleResponses, GetWakeScheduleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/schedules/{id}',
    ...options
});

/**
 * Update a wake schedule
 *
 * Update a schedule's command, args, cadence, addresses, note, or enabled
 * state. Changing the cadence (or re-enabling) recomputes the next fire time.
 *
 */
export const updateWakeSchedule = <ThrowOnError extends boolean = false>(options: Options<UpdateWakeScheduleData, ThrowOnError>) => (options.client ?? client).patch<UpdateWakeScheduleResponses, UpdateWakeScheduleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/schedules/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Run a wake schedule now
 *
 * Fire the schedule immediately, sending one wake.dispatch via the same
 * signed-send path as a scheduled fire. Does not change the schedule's next
 * fire time.
 *
 */
export const runWakeSchedule = <ThrowOnError extends boolean = false>(options: Options<RunWakeScheduleData, ThrowOnError>) => (options.client ?? client).post<RunWakeScheduleResponses, RunWakeScheduleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/schedules/{id}/run',
    ...options
});

/**
 * List wake authorizations
 *
 * Returns the per-target allowlist grants that authorize which senders may
 * wake a function. Optionally filter by the target endpoint.
 *
 */
export const listWakeAuthorizations = <ThrowOnError extends boolean = false>(options?: Options<ListWakeAuthorizationsData, ThrowOnError>) => (options?.client ?? client).get<ListWakeAuthorizationsResponses, ListWakeAuthorizationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/authorizations',
    ...options
});

/**
 * Create a wake authorization
 *
 * Grant a sender domain (and optionally a specific address and command set)
 * permission to wake a target function. The domain must be fully-qualified.
 *
 */
export const createWakeAuthorization = <ThrowOnError extends boolean = false>(options: Options<CreateWakeAuthorizationData, ThrowOnError>) => (options.client ?? client).post<CreateWakeAuthorizationResponses, CreateWakeAuthorizationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/authorizations',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a wake authorization
 */
export const deleteWakeAuthorization = <ThrowOnError extends boolean = false>(options: Options<DeleteWakeAuthorizationData, ThrowOnError>) => (options.client ?? client).delete<DeleteWakeAuthorizationResponses, DeleteWakeAuthorizationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/authorizations/{id}',
    ...options
});

/**
 * Update a wake authorization
 *
 * Toggle a wake authorization's enabled state.
 */
export const updateWakeAuthorization = <ThrowOnError extends boolean = false>(options: Options<UpdateWakeAuthorizationData, ThrowOnError>) => (options.client ?? client).patch<UpdateWakeAuthorizationResponses, UpdateWakeAuthorizationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/authorizations/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List recent wake dispatches
 *
 * Read-only audit of recent wake.dispatch interactions for the org.
 */
export const listWakeDispatches = <ThrowOnError extends boolean = false>(options?: Options<ListWakeDispatchesData, ThrowOnError>) => (options?.client ?? client).get<ListWakeDispatchesResponses, ListWakeDispatchesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wake/dispatches',
    ...options
});

/**
 * List recipient routes
 *
 * Returns the org's recipient routing rules in evaluation order. Each rule
 * binds a recipient address pattern to one endpoint; inbound mail resolves
 * to a single destination at delivery time.
 *
 */
export const listRoutes = <ThrowOnError extends boolean = false>(options?: Options<ListRoutesData, ThrowOnError>) => (options?.client ?? client).get<ListRoutesResponses, ListRoutesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/routes',
    ...options
});

/**
 * Create a recipient route
 *
 * Binds a recipient pattern to a destination. Provide exactly one of
 * `endpoint_id` (an existing endpoint) or `function_id`. With `function_id`,
 * a dedicated route-target endpoint is minted for that function in the same
 * transaction, enabling per-address function routing (e.g.
 * `alice@acme.com -> functionA`).
 *
 */
export const createRoute = <ThrowOnError extends boolean = false>(options: Options<CreateRouteData, ThrowOnError>) => (options.client ?? client).post<CreateRouteResponses, CreateRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/routes',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Reorder recipient routes
 *
 * Update the priority of one or more routes in a single call.
 */
export const reorderRoutes = <ThrowOnError extends boolean = false>(options: Options<ReorderRoutesData, ThrowOnError>) => (options.client ?? client).post<ReorderRoutesResponses, ReorderRoutesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/routes/reorder',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Simulate routing for a recipient
 *
 * Resolves where an inbound email to `recipient` would be delivered, with a
 * trace of every rule evaluated and why. Read-only; creates nothing.
 *
 */
export const simulateRoute = <ThrowOnError extends boolean = false>(options: Options<SimulateRouteData, ThrowOnError>) => (options.client ?? client).post<SimulateRouteResponses, SimulateRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/routes/simulate',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a recipient route
 */
export const deleteRoute = <ThrowOnError extends boolean = false>(options: Options<DeleteRouteData, ThrowOnError>) => (options.client ?? client).delete<DeleteRouteResponses, DeleteRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/routes/{id}',
    ...options
});

/**
 * Update a recipient route
 */
export const updateRoute = <ThrowOnError extends boolean = false>(options: Options<UpdateRouteData, ThrowOnError>) => (options.client ?? client).patch<UpdateRouteResponses, UpdateRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/routes/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List webhook deliveries
 *
 * Returns a paginated list of webhook delivery attempts. Each delivery
 * includes a nested `email` object with sender, recipient, and subject.
 *
 */
export const listDeliveries = <ThrowOnError extends boolean = false>(options?: Options<ListDeliveriesData, ThrowOnError>) => (options?.client ?? client).get<ListDeliveriesResponses, ListDeliveriesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/webhooks/deliveries',
    ...options
});

/**
 * Replay a webhook delivery
 *
 * Re-sends the stored webhook payload from a previous delivery attempt.
 * If the original endpoint is still active, it is targeted. If the
 * original endpoint was deleted, the oldest active endpoint is used.
 * Deactivated endpoints cannot be replayed to. Rate limited per-org,
 * sharing an org-wide budget with email replays.
 *
 */
export const replayDelivery = <ThrowOnError extends boolean = false>(options: Options<ReplayDeliveryData, ThrowOnError>) => (options.client ?? client).post<ReplayDeliveryResponses, ReplayDeliveryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/webhooks/deliveries/{id}/replay',
    ...options
});

/**
 * List send-permission rules
 *
 * Returns a flat list of rules describing every recipient the
 * caller may send to. Each rule has a `type`, a kind-specific
 * payload, and a human-readable `description`. If any rule
 * matches the recipient, /send-mail will accept the send under
 * the recipient-scope check.
 *
 * The endpoint is the answer to "where can I send" without
 * exposing internal entitlement names. Agents that don't
 * recognize a `type` can still read the `description` prose
 * and act on it.
 *
 * Rule kinds, ordered broadest-first so an agent can stop
 * scanning at the first match:
 *
 * 1. `any_recipient` (one entry, only when the org can send
 * anywhere): every other rule below it is redundant.
 * 2. `managed_zone` (always emitted, one per Primitive-managed
 * zone): sends to any address at *.primitive.email or
 * *.email.works always succeed; no entitlement required.
 * 3. `your_domain` (one per active verified outbound domain
 * owned by the org): sends to that domain are approved.
 * 4. `address` (one per address that has authenticated
 * inbound mail to the org, capped at `meta.address_cap`):
 * sends to that exact address are approved.
 *
 * The list is informational, not an authorization check.
 * /send-mail remains the source of truth on whether an
 * individual send will succeed (it also enforces the
 * from-address and the `send_mail` entitlement, which are
 * not recipient-scope concerns and are not represented here).
 *
 */
export const getSendPermissions = <ThrowOnError extends boolean = false>(options?: Options<GetSendPermissionsData, ThrowOnError>) => (options?.client ?? client).get<GetSendPermissionsResponses, GetSendPermissionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/send-permissions',
    ...options
});

/**
 * Send outbound email
 *
 * Sends an outbound email through Primitive's outbound relay. By default
 * the request returns once the relay accepts the message for delivery.
 * Set `wait: true` to wait for the first downstream SMTP delivery outcome.
 *
 * **Host routing.** /send-mail is served by the canonical API host
 * (`https://api.primitive.dev/v1`) so the request body can carry
 * inline attachments up to ~30 MiB raw. The legacy dashboard
 * compatibility host (`https://www.primitive.dev/api/v1`) also accepts
 * /send-mail, but Vercel request body limits apply before proxying.
 * The typed SDKs route /send-mail to the canonical API host
 * automatically.
 *
 */
export const sendEmail = <ThrowOnError extends boolean = false>(options: Options<SendEmailData, ThrowOnError>) => (options.client ?? client).post<SendEmailResponses, SendEmailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/send-mail',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Semantic search across received and sent mail
 *
 * Ranked search across both received and sent mail. The `mode`
 * field selects the ranking strategy:
 *
 * - `keyword`: lexical full-text matching only (no embeddings).
 * - `semantic`: meaning-based matching using vector embeddings.
 * - `hybrid` (default): blends the semantic and keyword signals.
 *
 * Results are ordered by a relevance `score`. Every row reports the
 * fields it matched (`matched_fields`), a match-centered excerpt per
 * field (`snippets`), and a `score_breakdown` whose components account
 * for the `score`. Page through results by passing the prior
 * response's `meta.cursor` back as `cursor`.
 *
 * Requires the Pro plan and the `semantic_search_enabled`
 * entitlement; callers without them receive `403`.
 *
 * Host routing: this operation is served only by the search host
 * (`https://api.primitive.dev/v1`). The typed SDKs route it there
 * automatically.
 *
 */
export const semanticSearch = <ThrowOnError extends boolean = false>(options: Options<SemanticSearchData, ThrowOnError>) => (options.client ?? client).post<SemanticSearchResponses, SemanticSearchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/semantic-search',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List outbound sent emails
 *
 * Returns a paginated list of OUTBOUND emails the caller's
 * org has sent via /send-mail (and /emails/{id}/reply, which
 * forwards through /send-mail). Includes every recorded
 * attempt, including gate-denied attempts that the agent
 * never called and rows still in `queued` state.
 *
 * For inbound mail received at your verified domains, see
 * /emails. There is no unified send/receive history endpoint;
 * the two surfaces are intentionally separate because the
 * underlying tables, statuses, and lifecycle differ.
 *
 * Email bodies (`body_text`, `body_html`) are NOT included on
 * list rows so a 50-row page can't balloon into a multi-MB
 * response when sends are near the 5MB body cap. Use
 * /sent-emails/{id} to fetch a single row with bodies, or
 * cross-reference by `client_idempotency_key` if the caller
 * already has the body locally.
 *
 */
export const listSentEmails = <ThrowOnError extends boolean = false>(options?: Options<ListSentEmailsData, ThrowOnError>) => (options?.client ?? client).get<ListSentEmailsResponses, ListSentEmailsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/sent-emails',
    ...options
});

/**
 * Get a sent email by id
 *
 * Returns the full sent-email record by id, including
 * `body_text` and `body_html` (omitted from the listing
 * endpoint to keep paginated responses small). Use this when
 * diagnosing a specific send, e.g. inspecting the receiver's
 * SMTP response on a `bounced` row or pulling the gate
 * denial detail on a `gate_denied` row.
 *
 */
export const getSentEmail = <ThrowOnError extends boolean = false>(options: Options<GetSentEmailData, ThrowOnError>) => (options.client ?? client).get<GetSentEmailResponses, GetSentEmailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/sent-emails/{id}',
    ...options
});

/**
 * Get a conversation thread by id
 *
 * Returns a conversation thread: its metadata plus the inbound
 * and outbound messages that belong to it, interleaved in time
 * order (oldest first). A thread spans both received emails and
 * your sends, so an agent can reconstruct an entire back-and-forth
 * from one call instead of walking reply headers.
 *
 * Each message carries a `direction` (`inbound` | `outbound`) and
 * an `id`; fetch the full message via `/emails/{id}` or
 * `/sent-emails/{id}` accordingly. Bodies are omitted here to keep
 * the thread view lightweight.
 *
 * Discover a thread id from the `thread_id` field on any email or
 * sent-email (list or detail). The message list is capped; compare
 * `message_count` against `messages.length` to detect truncation.
 *
 */
export const getThread = <ThrowOnError extends boolean = false>(options: Options<GetThreadData, ThrowOnError>) => (options.client ?? client).get<GetThreadResponses, GetThreadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/threads/{id}',
    ...options
});

/**
 * List functions
 *
 * Returns every active (non-deleted) function in the org, newest
 * first. Each entry carries deploy status and timestamps. To
 * inspect the source code or deploy errors, use `GET /functions/{id}`.
 *
 */
export const listFunctions = <ThrowOnError extends boolean = false>(options?: Options<ListFunctionsData, ThrowOnError>) => (options?.client ?? client).get<ListFunctionsResponses, ListFunctionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions',
    ...options
});

/**
 * Deploy a function
 *
 * Creates and deploys a new function. The handler must be a single
 * ESM module whose default export is an object with an async
 * `fetch(request, env)` method (Workers-style). Primitive signs
 * each delivery and forwards the `Primitive-Signature` header to
 * the handler. Verify the raw request body with
 * `PRIMITIVE_WEBHOOK_SECRET` before parsing JSON; after verification
 * the request body parses to a webhook event whose `event` field is
 * `email.received` for normal inbound mail, or a machine-mail type
 * (`email.bounced`, `email.tls_report`, `email.dmarc_report`,
 * `email.dmarc_failure`) for bounces and reports. Code is bundled
 * before being uploaded; ship a single self-contained file rather
 * than relying on external imports.
 *
 * **Code limits.** `code` is capped at 1 MiB UTF-8. `sourceMap`
 * (optional) is capped at 5 MiB UTF-8, stored with each deployment
 * attempt, and sent to the runtime so stack traces can resolve to
 * original source files.
 *
 * **Routing.** On successful deploy, the function code is live
 * in the runtime, but inbound mail will not reach it until at
 * least one route is bound. Routes are managed from the Primitive
 * dashboard. A `deploy_status` of `deployed` means the script is
 * installed, not that the function is receiving mail. The
 * internal runtime URL is not returned by the API and is not a
 * customer-facing integration surface.
 *
 * **Secrets.** New functions ship with the managed secrets
 * (`PRIMITIVE_WEBHOOK_SECRET`, `PRIMITIVE_API_KEY`,
 * `PRIMITIVE_API_BASE_URL`) already bound. Add user-set secrets via
 * `POST /functions/{id}/secrets`; secret writes only land in the
 * running handler on the next redeploy.
 *
 */
export const createFunction = <ThrowOnError extends boolean = false>(options: Options<CreateFunctionData, ThrowOnError>) => (options.client ?? client).post<CreateFunctionResponses, CreateFunctionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a function
 *
 * Soft-deletes the function row, removes the script from the edge
 * runtime, and deactivates any route bound to this function so no
 * further inbound mail is delivered. Past deploy history,
 * invocations, and logs are retained.
 *
 * Returns 502 if the runtime delete fails partway; the function
 * row stays in place and the call is safe to retry until it
 * succeeds.
 *
 */
export const deleteFunction = <ThrowOnError extends boolean = false>(options: Options<DeleteFunctionData, ThrowOnError>) => (options.client ?? client).delete<DeleteFunctionResponses, DeleteFunctionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}',
    ...options
});

/**
 * Get a function
 *
 * Returns the full record for a function, including its current
 * source code and the deploy status / error from the most recent
 * deploy attempt.
 *
 */
export const getFunction = <ThrowOnError extends boolean = false>(options: Options<GetFunctionData, ThrowOnError>) => (options.client ?? client).get<GetFunctionResponses, GetFunctionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}',
    ...options
});

/**
 * Update and redeploy a function
 *
 * Replaces the function's source code with the body's `code` and
 * triggers a redeploy. Same size limits as `POST /functions`.
 * Use this verb to push secret writes into the running handler:
 * passing the same `code` re-runs the deploy and refreshes the
 * binding set with the latest values from the secrets table.
 *
 * On deploy failure, the previously-deployed code stays live; the
 * runtime never serves a half-built bundle. The response uses
 * `error.code` `deploy_failed`, and the function's `deploy_error`
 * field carries the latest deploy error for dashboard/API reads.
 *
 */
export const updateFunction = <ThrowOnError extends boolean = false>(options: Options<UpdateFunctionData, ThrowOnError>) => (options.client ?? client).put<UpdateFunctionResponses, UpdateFunctionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Send a test invocation
 *
 * Sends a real test email from a Primitive-controlled sender to a
 * local-part on one of the org's verified inbound domains. By
 * default the recipient is a synthetic
 * `__primitive_function_test+<random>@<domain>` address on a
 * domain selected to route to the function. Scoped functions use
 * their scoped domain; fallback functions use a domain that has
 * no enabled domain-scoped endpoint. Pass `local_part` to
 * override and exercise routing logic that branches on a specific
 * recipient (the common pattern when one function handles multiple
 * inboxes like `summarize@` and `action@`). The function fires
 * through the normal MX delivery path, so reply / send-mail calls
 * from inside the handler against the inbound's `email.id` work
 * the same as in production. Returns immediately after the send is
 * queued; the invocation appears on the function's invocations
 * list within a few seconds.
 *
 * Requires that the function is currently `deployed`. Returns 422
 * if the function is in `pending` or `failed` state, or if the
 * org has no verified inbound domain to receive the test mail.
 * Returns 400 if `local_part` is set to a value that does not
 * match the local-part character set.
 *
 */
export const testFunction = <ThrowOnError extends boolean = false>(options: Options<TestFunctionData, ThrowOnError>) => (options.client ?? client).post<TestFunctionResponses, TestFunctionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/test',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Get a function test run trace
 *
 * Returns the current end-to-end trace for a function test run.
 * The trace is intentionally partial while the test is still in
 * flight: callers can poll this endpoint and watch it fill in
 * from send -> inbound -> webhook deliveries -> outbound
 * requests, logs, and replies.
 *
 */
export const getFunctionTestRunTrace = <ThrowOnError extends boolean = false>(options: Options<GetFunctionTestRunTraceData, ThrowOnError>) => (options.client ?? client).get<GetFunctionTestRunTraceResponses, GetFunctionTestRunTraceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/test-runs/{run_id}/trace',
    ...options
});

/**
 * Get the org's function routing topology
 *
 * Returns a single snapshot of how inbound mail is routed across
 * this org's active domains and functions: which active domain has
 * which function bound, the org's fallback function (if any), and
 * every deployed function with no route bound. Use this to answer
 * "which of my functions actually receive mail?" diagnostically.
 *
 */
export const getOrgRoutingTopology = <ThrowOnError extends boolean = false>(options?: Options<GetOrgRoutingTopologyData, ThrowOnError>) => (options?.client ?? client).get<GetOrgRoutingTopologyResponses, GetOrgRoutingTopologyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/routing-topology',
    ...options
});

/**
 * Get a function's current route binding
 *
 * Returns the endpoint binding for the function, or null when no
 * route is currently bound. The binding identifies whether the
 * function receives mail for a specific domain (scoped) or for any
 * active domain that has no scoped binding (fallback).
 *
 */
export const getFunctionRouting = <ThrowOnError extends boolean = false>(options: Options<GetFunctionRoutingData, ThrowOnError>) => (options.client ?? client).get<GetFunctionRoutingResponses, GetFunctionRoutingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/routing',
    ...options
});

/**
 * Unbind any route from a function
 *
 * Deactivates every active endpoint bound to this function. The
 * function stays deployed but stops receiving inbound mail. Safe
 * to call when no route is currently bound (no-op).
 *
 */
export const unsetFunctionRoute = <ThrowOnError extends boolean = false>(options: Options<UnsetFunctionRouteData, ThrowOnError>) => (options.client ?? client).delete<UnsetFunctionRouteResponses, UnsetFunctionRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/route',
    ...options
});

/**
 * Bind a route to a function
 *
 * Binds inbound mail to this function. The route target is either
 * a specific verified domain (scoped) or the org's fallback (any
 * active domain with no scoped binding). If another function is
 * already bound at the target, returns a `conflict` envelope
 * describing the holder; re-issue with `takeover: true` to
 * deactivate that prior binding and install this one.
 *
 */
export const setFunctionRoute = <ThrowOnError extends boolean = false>(options: Options<SetFunctionRouteData, ThrowOnError>) => (options.client ?? client).put<SetFunctionRouteResponses, SetFunctionRouteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/route',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List a function's secrets
 *
 * Returns metadata for every secret bound to the function, with
 * managed entries (provisioned by Primitive) listed first and
 * user-set entries listed alphabetically after. **Values are
 * never returned.** Secret writes are write-only.
 *
 * Managed entries (e.g. `PRIMITIVE_WEBHOOK_SECRET`,
 * `PRIMITIVE_API_KEY`, `PRIMITIVE_API_BASE_URL`) carry a
 * `description` instead of `created_at` / `updated_at`. They
 * cannot be created, updated, or deleted via this API.
 *
 */
export const listFunctionSecrets = <ThrowOnError extends boolean = false>(options: Options<ListFunctionSecretsData, ThrowOnError>) => (options.client ?? client).get<ListFunctionSecretsResponses, ListFunctionSecretsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/secrets',
    ...options
});

/**
 * Create or update a secret
 *
 * Idempotent insert-or-update keyed on `(function_id, key)`.
 * Returns 201 the first time the key is set, 200 on subsequent
 * updates. Values are encrypted at rest and only become visible
 * to the running handler on the next deploy (`PUT /functions/{id}`
 * with the existing code is sufficient to refresh bindings).
 *
 * Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,
 * digits, underscores; first character is a letter or
 * underscore). Values are at most 4096 UTF-8 bytes. System-
 * managed keys are reserved and rejected.
 *
 */
export const createFunctionSecret = <ThrowOnError extends boolean = false>(options: Options<CreateFunctionSecretData, ThrowOnError>) => (options.client ?? client).post<CreateFunctionSecretResponses, CreateFunctionSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/secrets',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a secret
 *
 * Removes the secret. The binding stays live in the running
 * handler until the next deploy refreshes the binding set
 * (`PUT /functions/{id}` with the existing code is sufficient).
 * Returns 404 if the key did not exist. Managed system keys
 * cannot be deleted.
 *
 */
export const deleteFunctionSecret = <ThrowOnError extends boolean = false>(options: Options<DeleteFunctionSecretData, ThrowOnError>) => (options.client ?? client).delete<DeleteFunctionSecretResponses, DeleteFunctionSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/secrets/{key}',
    ...options
});

/**
 * Set a secret by key
 *
 * Path-keyed companion to `POST /functions/{id}/secrets`.
 * Idempotent: returns 201 the first time the key is set, 200 on
 * subsequent updates. Same validation rules and same write-only
 * guarantees as the POST verb; the new value lands in the running
 * handler on the next deploy.
 *
 */
export const setFunctionSecret = <ThrowOnError extends boolean = false>(options: Options<SetFunctionSecretData, ThrowOnError>) => (options.client ?? client).put<SetFunctionSecretResponses, SetFunctionSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/secrets/{key}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List org-level (global) secrets
 *
 * Returns metadata for every org-level secret. Org secrets apply
 * to every function in the org and are read as `env.<KEY>` in
 * handlers. **Values are never returned.** Secret writes are
 * write-only. A function-level secret of the same name overrides
 * the org-level value for that function.
 *
 */
export const listOrgSecrets = <ThrowOnError extends boolean = false>(options?: Options<ListOrgSecretsData, ThrowOnError>) => (options?.client ?? client).get<ListOrgSecretsResponses, ListOrgSecretsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/org/secrets',
    ...options
});

/**
 * Create or update an org secret
 *
 * Idempotent insert-or-update keyed on `(org_id, key)`. Returns
 * 201 the first time the key is set, 200 on subsequent updates.
 * Values are encrypted at rest. A changed value lands in a
 * function only on that function's next deploy.
 *
 * Keys must match `^[A-Z_][A-Z0-9_]*$` (uppercase letters,
 * digits, underscores; first character is a letter or
 * underscore). Values are at most 4096 UTF-8 bytes. System-
 * managed keys are reserved and rejected.
 *
 */
export const createOrgSecret = <ThrowOnError extends boolean = false>(options: Options<CreateOrgSecretData, ThrowOnError>) => (options.client ?? client).post<CreateOrgSecretResponses, CreateOrgSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/org/secrets',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete an org secret
 *
 * Removes the org secret. Functions keep the previous value until
 * each is redeployed. Returns 404 if the key did not exist.
 *
 */
export const deleteOrgSecret = <ThrowOnError extends boolean = false>(options: Options<DeleteOrgSecretData, ThrowOnError>) => (options.client ?? client).delete<DeleteOrgSecretResponses, DeleteOrgSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/org/secrets/{key}',
    ...options
});

/**
 * Set an org secret by key
 *
 * Path-keyed companion to `POST /org/secrets`. Idempotent:
 * returns 201 the first time the key is set, 200 on subsequent
 * updates. Same validation and write-only guarantees as POST.
 *
 */
export const setOrgSecret = <ThrowOnError extends boolean = false>(options: Options<SetOrgSecretData, ThrowOnError>) => (options.client ?? client).put<SetOrgSecretResponses, SetOrgSecretErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/org/secrets/{key}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List a function's execution logs
 *
 * Returns the most recent `function_logs` rows for the function,
 * newest first. Each row is a single `console.log` / `console.error`
 * invocation captured from the running handler.
 *
 * Page through history with the opaque `cursor` returned as
 * `next_cursor`; pass it back as the `cursor` query param on the
 * next call. `next_cursor` is `null` when there are no further
 * rows. The cursor format is an implementation detail and should
 * not be parsed by callers.
 *
 */
export const listFunctionLogs = <ThrowOnError extends boolean = false>(options: Options<ListFunctionLogsData, ThrowOnError>) => (options.client ?? client).get<ListFunctionLogsResponses, ListFunctionLogsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/functions/{id}/logs',
    ...options
});

/**
 * Delete a memory
 *
 * Delete one active memory by key and scope. Deletes are idempotent when
 * `if_version` is omitted: deleting a missing key returns `deleted:
 * false`. With `if_version`, a missing key still returns `deleted: false`,
 * but a stale version returns `memory_conflict`.
 *
 * A successful delete records memory write usage.
 *
 */
export const deleteMemory = <ThrowOnError extends boolean = false>(options: Options<DeleteMemoryData, ThrowOnError>) => (options.client ?? client).delete<DeleteMemoryResponses, DeleteMemoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/memories',
    ...options
});

/**
 * Get a memory
 *
 * Fetch one active memory by key and scope. Omit scope parameters to use
 * the automatic default: function-authenticated context, then the
 * `x-primitive-function-id` header, then org scope. Function scope uses a
 * function id UUID in `scope_id`.
 *
 * A successful read records memory read usage and updates the memory's
 * read stats asynchronously.
 *
 */
export const getMemory = <ThrowOnError extends boolean = false>(options: Options<GetMemoryData, ThrowOnError>) => (options.client ?? client).get<GetMemoryResponses, GetMemoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/memories',
    ...options
});

/**
 * Set a memory
 *
 * Create or update a durable JSON memory under an org or function scope.
 * When no explicit scope is provided, function-authenticated requests
 * use that function's id automatically; requests with
 * `x-primitive-function-id` use that function id; all other requests
 * default to org scope.
 *
 * `scope.type = function` requires the function id UUID in `scope.id`.
 * Function names are not accepted as scope identifiers. Values must be
 * valid JSON and serialize to at most 65536 UTF-8 bytes. Keys must be at
 * most 512 UTF-8 bytes. `version`, `read_count`, and `write_count` are
 * bigint counters serialized as strings.
 *
 * Passing `if_absent` turns the write into create-only. Passing
 * `if_version` turns the write into compare-and-set. These options are
 * mutually exclusive and return `memory_conflict` on a stale version or
 * existing key.
 *
 */
export const setMemory = <ThrowOnError extends boolean = false>(options: Options<SetMemoryData, ThrowOnError>) => (options.client ?? client).put<SetMemoryResponses, SetMemoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/memories',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Search memories
 *
 * List active memories in a scope by lexicographic key prefix. Results
 * are ordered by key ascending. The `meta.cursor` value is the next key
 * cursor; pass it back as `cursor` to continue after that key.
 *
 * Search records one memory read usage event for the operation. Pass
 * `include_value=false` to return metadata only.
 *
 */
export const searchMemories = <ThrowOnError extends boolean = false>(options?: Options<SearchMemoriesData, ThrowOnError>) => (options?.client ?? client).get<SearchMemoriesResponses, SearchMemoriesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/memories/search',
    ...options
});

/**
 * List payout addresses
 *
 * List your org's registered payout addresses, newest first.
 */
export const listPayoutAddresses = <ThrowOnError extends boolean = false>(options?: Options<ListPayoutAddressesData, ThrowOnError>) => (options?.client ?? client).get<ListPayoutAddressesResponses, ListPayoutAddressesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/payout-addresses',
    ...options
});

/**
 * Register a payout address
 *
 * Register (or update) the default payout address your org receives x402
 * payments at, for a given network. You prove control of the address with
 * an org-bound `personal_sign` signature over the message produced by the
 * SDK helper `buildPayoutRegistrationMessage`. The org id is taken from your
 * authenticated key, never the body, so a captured signature can't register
 * an address under another org. Exactly one default address exists per
 * (org, network); registering again replaces it. A payee MUST register a
 * payout address before calling `createChallenge`, because the challenge's
 * `pay_to` is resolved from this directory.
 *
 */
export const registerPayoutAddress = <ThrowOnError extends boolean = false>(options: Options<RegisterPayoutAddressData, ThrowOnError>) => (options.client ?? client).post<RegisterPayoutAddressResponses, RegisterPayoutAddressErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/payout-addresses',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Create an email-native payment challenge
 *
 * Issue an x402 payment challenge over a real email thread (the payee
 * side). Unlike `createChallenge` (which mints a synthetic challenge id),
 * this sends the challenge as an email from `from` to `to` and binds the
 * payment to that DKIM-authenticated thread. The `pay_to` address and the
 * token asset are resolved server-side from your registered default payout
 * address for the network, never from the request. The response carries
 * the thread's `interaction_id` plus the `challenge` (the
 * `payment_requirements`, the `nonce_binding`, and `expires_at`) the payer
 * needs to sign; the payer replies with a signed `payment` interaction
 * step. Amounts are in token base units (USDC has 6 decimals, so `"10000"`
 * is 0.01 USDC).
 *
 */
export const createEmailChallenge = <ThrowOnError extends boolean = false>(options: Options<CreateEmailChallengeData, ThrowOnError>) => (options.client ?? client).post<CreateEmailChallengeResponses, CreateEmailChallengeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/email-challenges',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Create a payment challenge
 *
 * Create an x402 payment challenge (the payee side of a payment). The
 * `pay_to` address is resolved server-side from your registered default
 * payout address for the network, never from the request. The response
 * carries the `nonce_binding` and `payment_requirements` the payer needs to
 * sign; hand the whole challenge object to the payer (for example in an
 * email reply). Amounts are in token base units (USDC has 6 decimals, so
 * `"10000"` is 0.01 USDC).
 *
 */
export const createChallenge = <ThrowOnError extends boolean = false>(options: Options<CreateChallengeData, ThrowOnError>) => (options.client ?? client).post<CreateChallengeResponses, CreateChallengeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/challenges',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Get a payment challenge
 *
 * Fetch a challenge you created, to poll its `status` and settlement
 * receipt (`settle_tx`). Scoped to the challenger org that created it.
 *
 */
export const getChallenge = <ThrowOnError extends boolean = false>(options: Options<GetChallengeData, ThrowOnError>) => (options.client ?? client).get<GetChallengeResponses, GetChallengeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/challenges/{id}',
    ...options
});

/**
 * Pay a payment challenge
 *
 * Settle a challenge addressed to your org as payer. The request body
 * carries a signed x402 `PaymentPayload`: an EIP-3009
 * `transferWithAuthorization` signed locally with your own key, whose nonce
 * is bound to the challenge via the SDK's `deriveEip3009Nonce`. The platform
 * verifies every signed field against its own record of the challenge,
 * applies your spend policy, and settles on-chain through a facilitator.
 * Settlement is non-custodial; Primitive never holds funds. Idempotent:
 * paying an already-settled challenge returns the original receipt. Most
 * callers use the SDK `pay()` helper rather than building the payload by
 * hand.
 *
 */
export const payChallenge = <ThrowOnError extends boolean = false>(options: Options<PayChallengeData, ThrowOnError>) => (options.client ?? client).post<PayChallengeResponses, PayChallengeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/challenges/{id}/pay',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Get your spend policy
 *
 * Read your org's outbound spend policy: the kill-switch, per-payment and
 * per-day caps, and the payee allowlist. Returns the defaults (no limits,
 * not paused) when no policy has been set.
 *
 */
export const getSpendPolicy = <ThrowOnError extends boolean = false>(options?: Options<GetSpendPolicyData, ThrowOnError>) => (options?.client ?? client).get<GetSpendPolicyResponses, GetSpendPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/spend-policy',
    ...options
});

/**
 * Update your spend policy
 *
 * Update your org's spend policy. Applied as a merge: only the fields you
 * include change, and omitted fields keep their current value, so a partial
 * update can't silently reset the kill-switch. Send an explicit `null` to
 * clear a cap. Caps are in token base units.
 *
 */
export const updateSpendPolicy = <ThrowOnError extends boolean = false>(options: Options<UpdateSpendPolicyData, ThrowOnError>) => (options.client ?? client).put<UpdateSpendPolicyResponses, UpdateSpendPolicyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/spend-policy',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List declined payments
 *
 * The 50 most recent payments your org's spend policy declined, newest
 * first. Use this to see why an outbound payment was refused (a cap, the
 * payee allowlist, or the kill-switch) instead of only reading the
 * dashboard.
 *
 */
export const listDeclinedPayments = <ThrowOnError extends boolean = false>(options?: Options<ListDeclinedPaymentsData, ThrowOnError>) => (options?.client ?? client).get<ListDeclinedPaymentsResponses, ListDeclinedPaymentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/x402/declined-payments',
    ...options
});

/**
 * List the registries you own
 */
export const listRegistries = <ThrowOnError extends boolean = false>(options?: Options<ListRegistriesData, ThrowOnError>) => (options?.client ?? client).get<ListRegistriesResponses, ListRegistriesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries',
    ...options
});

/**
 * Create a registry
 */
export const createRegistry = <ThrowOnError extends boolean = false>(options: Options<CreateRegistryData, ThrowOnError>) => (options.client ?? client).post<CreateRegistryResponses, CreateRegistryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Delete a registry you own
 *
 * Removes the registry from discovery and frees its slug for re-creation.
 */
export const deleteRegistry = <ThrowOnError extends boolean = false>(options: Options<DeleteRegistryData, ThrowOnError>) => (options.client ?? client).delete<DeleteRegistryResponses, DeleteRegistryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries/{slug}',
    ...options
});

/**
 * Get a public registry's metadata
 */
export const getRegistry = <ThrowOnError extends boolean = false>(options: Options<GetRegistryData, ThrowOnError>) => (options.client ?? client).get<GetRegistryResponses, GetRegistryErrors, ThrowOnError>({ url: '/registries/{slug}', ...options });

/**
 * Update a registry you own
 */
export const updateRegistry = <ThrowOnError extends boolean = false>(options: Options<UpdateRegistryData, ThrowOnError>) => (options.client ?? client).patch<UpdateRegistryResponses, UpdateRegistryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries/{slug}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * List agents in a registry
 */
export const listRegistryAgents = <ThrowOnError extends boolean = false>(options: Options<ListRegistryAgentsData, ThrowOnError>) => (options.client ?? client).get<ListRegistryAgentsResponses, unknown, ThrowOnError>({ url: '/registries/{slug}/agents', ...options });

/**
 * Publish an agent into a registry
 */
export const publishAgent = <ThrowOnError extends boolean = false>(options: Options<PublishAgentData, ThrowOnError>) => (options.client ?? client).post<PublishAgentResponses, PublishAgentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries/{slug}/agents',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Unpublish an agent from a registry
 */
export const unpublishAgent = <ThrowOnError extends boolean = false>(options: Options<UnpublishAgentData, ThrowOnError>) => (options.client ?? client).delete<UnpublishAgentResponses, UnpublishAgentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries/{slug}/agents/{handle}',
    ...options
});

/**
 * Resolve a registry handle to its agent
 */
export const resolveRegistryHandle = <ThrowOnError extends boolean = false>(options: Options<ResolveRegistryHandleData, ThrowOnError>) => (options.client ?? client).get<ResolveRegistryHandleResponses, ResolveRegistryHandleErrors, ThrowOnError>({ url: '/registries/{slug}/agents/{handle}', ...options });

/**
 * List pending publication requests
 */
export const listRegistryRequests = <ThrowOnError extends boolean = false>(options: Options<ListRegistryRequestsData, ThrowOnError>) => (options.client ?? client).get<ListRegistryRequestsResponses, ListRegistryRequestsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries/{slug}/requests',
    ...options
});

/**
 * Approve or reject a publication request
 */
export const decideRegistryRequest = <ThrowOnError extends boolean = false>(options: Options<DecideRegistryRequestData, ThrowOnError>) => (options.client ?? client).post<DecideRegistryRequestResponses, DecideRegistryRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/registries/{slug}/requests/{id}',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Define an agent identity
 */
export const defineAgent = <ThrowOnError extends boolean = false>(options: Options<DefineAgentData, ThrowOnError>) => (options.client ?? client).post<DefineAgentResponses, DefineAgentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/agents',
    ...options,
    headers: {
        ...(options.body !== undefined && { 'Content-Type': 'application/json' }),
        ...options.headers
    }
});

/**
 * Get an agent's public profile by address
 */
export const getAgent = <ThrowOnError extends boolean = false>(options: Options<GetAgentData, ThrowOnError>) => (options.client ?? client).get<GetAgentResponses, GetAgentErrors, ThrowOnError>({ url: '/agents/{address}', ...options });
